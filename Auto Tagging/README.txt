1.Data Preprocessing consist of R file used for per processing the data
	a.Keep the file to be processed as input.csv in same folder
	b.Run the code using Rstudio
	c.Processed file will be in same folder with name processed.csv

2.Classifier folder consist of all classiers code
	a.classifiers.py(requires python and scikit library to run) consist of Ridge,Naive Bayes and Linear SVC implemetation using scikit library
	b.KnnClassifier.ipynb (requires ipython notebook  and graphlab to run), contains graphlan KNN implementation and our tfidf and Knn classifier	
	c.To run any classifier put the processed.csv from step 1 to same location where code is located